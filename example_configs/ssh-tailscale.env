# SSH with Tailscale enabled
# Tests: SSH + Tailscale integration, external SSH via Tailscale

TAILSCALE_ENABLE=true
ENABLE_NGROK=false
ENABLE_SPACES=false
SSH_ENABLE=true
ENABLE_UI=true
STABLE_HOSTNAME=ssh-tailscale
S6_BEHAVIOUR_IF_STAGE2_FAILS=0
